// Methods handling modal opening

import {
  addModalToStack,
  addModalToQueue,
  stack,
  queue,
  displayModal,
  closeModal
} from './utils.js';

export { addModalToStack, addModalToQueue, stack, queue, displayModal, closeModal };
